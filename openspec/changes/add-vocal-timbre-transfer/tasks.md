# Tasks: add-vocal-timbre-transfer

> 实现清单。每完成一项将 `- [ ]` 改为 `- [x]`。apply 阶段按序推进。

## 1. 项目脚手架与环境

- [x] 1.1 初始化 Python 项目结构（`src/musicremix/` 包、`pyproject.toml`、`README.md`、`.gitignore`）
- [x] 1.2 配置依赖管理（pyproject.toml 声明 torch、demucs、librosa、soundfile、pydub、typer、fastapi 等依赖，锁定版本范围）
- [x] 1.3 配置开发工具链（ruff/black 格式化、pytest 测试框架、pre-commit 可选）
- [x] 1.4 建立本地模型缓存目录约定（`~/.musicremix/models`）与配置加载模块

## 2. 通用基础设施

- [x] 2.1 实现设备检测工具（CUDA / Apple MPS / CPU 自动选择，含 CPU 回退提示）
- [x] 2.2 实现音频 I/O 工具（wav/mp3/flac 读取、写入、重采样、格式转换）
- [x] 2.3 实现模型懒加载与本地缓存管理（首次下载、复用、路径解析）
- [x] 2.4 实现分片处理工具（长音频切片 + 无缝拼接，控制内存峰值）

## 3. 人声分离模块（vocal-extraction）

- [x] 3.1 定义 `SourceSeparator` 抽象接口（输入音频路径，输出人声/伴奏轨）
- [x] 3.2 实现 Demucs 后端（htdemucs 2 轨分离：人声 + 伴奏）
- [x] 3.3 支持仅输出人声模式与可配置输出采样率
- [x] 3.4 实现输入格式兼容（mp3/flac/wav 解码 + 非标准采样率内部重采样）
- [x] 3.5 编写人声分离单元测试（格式兼容、时长一致、设备回退）

## 4. 目标音色建模模块（reference-voice-modeling）

- [x] 4.1 定义 `VoiceModeler` 抽象接口（输入参考人声素材，输出可持久化音色索引）
- [x] 4.2 实现参考歌曲人声提取（复用分离模块得到歌手 B 干净人声）
- [x] 4.3 实现 RVC 内容编码特征提取与检索索引构建（生成 .index 文件）
- [x] 4.4 实现多首参考特征融合（合并多首人声特征到同一索引）
- [x] 4.5 实现索引缓存与复用（按目标歌手标识缓存，跳过重复构建）
- [x] 4.6 实现参考素材质量校验（时长过短/静音占比过高告警，不中断流程）
- [x] 4.7 编写音色建模单元测试（索引生成、多参考融合、缓存复用）

## 5. 音色迁移模块（timbre-transfer）

- [x] 5.1 定义 `VoiceConverter` 抽象接口（输入源人声 + 目标索引，输出转换人声）
- [x] 5.2 实现 RVC 转换后端（加载索引 + 基础模型，执行检索式音色迁移）
- [x] 5.3 接入音高算法（crepe/rmvpe）保证旋律与音高保持
- [x] 5.4 暴露可配置参数（index_rate、f0 算法、pitch 偏移），设置自然度优先默认值
- [x] 5.5 实现长歌曲分片转换 + 无缝拼接（复用 2.4 工具）
- [x] 5.6 编写音色迁移单元测试（时长一致、参数生效、分片拼接连续性）

## 6. 混音合成模块

- [x] 6.1 定义 `Remixer` 抽象接口（输入转换人声 + 原曲伴奏，输出合成歌曲）
- [x] 6.2 实现基于伴奏的时长对齐（重采样/裁齐，消除轻微错位）
- [x] 6.3 实现合成输出（人声 + 伴奏混合，可配置人声/伴奏音量比）
- [x] 6.4 实现输出格式与采样率配置（默认 wav 无损，支持指定采样率）
- [x] 6.5 编写混音合成单元测试（同步对齐、格式输出、音量配比）

## 7. 端到端流水线编排（song-remix-pipeline）

- [x] 7.1 实现流水线编排器（串联 分离→建模→迁移→合成，定义阶段间数据契约）
- [x] 7.2 实现中间产物保留（各阶段输出落盘到工作目录，便于排查与复用）
- [x] 7.3 实现阶段进度输出（每阶段开始/完成日志，便于观测运行状态）
- [x] 7.4 实现失败续跑（检测已缓存中间产物，跳过已完成阶段，从失败处继续）
- [x] 7.5 实现错误处理与清晰报错（标注失败阶段 + 已完成阶段 + 续跑提示）
- [x] 7.6 编写端到端集成测试（用小样音频跑通全流程，校验最终输出存在且可播放）

## 8. CLI 入口

- [x] 8.1 实现主命令 `musicremix remix`（必填：输入歌曲、参考歌曲、输出路径）
- [x] 8.2 实现可选参数（输出格式、采样率、index_rate、f0、音量比、工作目录）
- [x] 8.3 实现子命令拆分（`separate` / `model` / `convert` / `mix` 可独立调用）
- [x] 8.4 实现设备/缓存查询命令（`musicremix info` 显示设备与缓存状态）
- [x] 8.5 编写 CLI 冒烟测试（参数解析、默认值、帮助文本）

## 9. 文档与验收

- [x] 9.1 编写 README（安装、GPU 依赖说明、快速开始示例、许可证声明）
- [x] 9.2 编写参数调优指南（index_rate/f0/音量比对效果的影响与推荐值）
- [x] 9.3 整理各依赖开源许可证清单，标注商用注意事项
- [ ] 9.4 端到端人工验收（用真实歌曲跑通：歌手 A 歌曲 + 歌手 B 参考 → 输出换音色歌曲，主观评估自然度）
